% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe_extract_latent.R
\name{step_extract_latent}
\alias{step_extract_latent}
\title{Extract latent features using Self-Supervised Learning methods}
\usage{
step_extract_latent(
  recipe,
  ...,
  role = "predictor",
  trained = FALSE,
  pretraining_type = "SCARF",
  create_validation = FALSE,
  validation_proportion = 0.1,
  batch_size = 256,
  epochs = 150,
  batch_size_inference = 32,
  pretrained_model = NULL,
  skip = FALSE,
  id = rand_id("extract_latent")
)
}
\arguments{
\item{recipe}{A recipe object. The step will be added to the sequence of
operations for this recipe}

\item{...}{One or more selector expressions to choose which variables will be
used to train the SSL model and extract features}

\item{role}{A character string specifying the role of the newly generated
variables. Defaults to \code{"predictor"}.}

\item{trained}{A logical to indicate if the quantities for preprocessing have
been estimated.}

\item{pretraining_type}{A character specifying the SSL method to use.
Default is \code{'SCARF'}.}

\item{create_validation}{A \code{logical}. If \code{TRUE}, splits the
training data to create a validation set. Default is \code{FALSE}.}

\item{validation_proportion}{\code{Numeric}. Proportion of data (0 to 1)
allocated for validation if \code{create_validation = TRUE}. Default is
\code{0.1}.}

\item{batch_size}{An \code{integer} defining the number of samples per batch
during training. Default is \code{256}.}

\item{epochs}{An \code{integer} defining the number of training epochs.
Default is \code{150}.}

\item{batch_size_inference}{An integer specifying the batch size during the
inference or transformation phase (\code{bake}). Default is \code{32}}

\item{pretrained_model}{The pretrained model object once the step has been
executed by \code{prep()}.}

\item{skip}{A logical. Should the step be skipped when the recipe is baked
by \code{bake()}? Defaults to \code{FALSE} so that the transformation is
applied to both training and test sets.}

\item{id}{A character string that is unique to this step to identify it.}
}
\value{
An updated recipe object with the class \code{step_extract_latent}
added to the sequence of operations.
}
\description{
Extract latent features using Self-Supervised Learning methods
}
\details{
The algorithms that can be used for feature extraction require each predictor
 to be numerical. For that reason, if your dataset
contains categorical data, before using this step, you must:
\itemize{
  \item Impute missing values (i.e. \code{step_impute_median()})
  \item Convert categorical data to numerical data (i.e.
  \code{step_dummy(..., one_hot = TRUE)})
  \item Normalize numerical data (i.e. \code{step_nomalize()})
}
}
\examples{
a <- 1
}
